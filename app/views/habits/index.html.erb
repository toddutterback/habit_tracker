<h1>21 days to create a habit</h1>
<br />

<%= link_to "Add a Habit", new_habit_path, class: "btn" %>

<% @habits.each do |habit| %>
  <div class="row">
    <div class="col s12 m6">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <span class="card-title"><%= habit.name %></span>
          <%= link_to  'View Habit', habit_path(habit) %>

            <!-- <div class = 'row'> -->
              <!-- <div class = "input-field col s12">

                <%= form_for habit do |f| %>
                        <%= f.check_box :completed, class: "filled-in", id: habit.id %>
                        <%= f.label :completed, for: habit.id %>
                        <%= f.submit 'Save', class: 'btn-flat white-text btn-border right' %>
                  <% end %>
              </div>
            </div> -->


        <div class="card-action">
          <%= link_to  'Edit Habit', edit_habit_path(habit) %>
          <%= link_to 'Delete Habit', habit_path(habit), method: :delete %>
        </div>
      </div>
    </div>
  </div>
<% end %>
